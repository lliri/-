
y = [0,0,0,0]
dy =[2,5,9,3]
def setup():
    size(500,500)
def draw():
    background(255)
    global lst,index
    fill(0)
    stroke(0)
    strokeWeight(200)
    for i in range(len(y)):
        circle( 20 + i*40 , y[i] , 50)
        y[i] += dy[i]
